name: Ecosystem compatibility

on:
  workflow_dispatch:
  pull_request:
    paths:
      - ".github/workflows/ecosystem.yml"
      - "Examples/Corpus/**"
      - "Packaging/EcosystemCompatibility.json"
      - "Scripts/validate-ecosystem-contract.mjs"
      - "Scripts/validate-ecosystem-integration.sh"
      - "Validation/Ecosystem/**"
  schedule:
    - cron: "17 3 * * 1"

concurrency:
  group: ecosystem-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  compiler-compatibility:
    runs-on: macos-15
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: swift-actions/setup-swift@v2
        with:
          swift-version: "6.0"
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Validate pinned Compiler without source changes
        env:
          ECOSYSTEM_REPOSITORIES: Compiler
          ECOSYSTEM_SKIP_WORKBENCH: 1
          ECOSYSTEM_REPORT_PATH: artifacts/compiler-compatibility.json
        run: Scripts/validate-ecosystem-integration.sh
      - uses: actions/upload-artifact@v4
        with:
          name: compiler-compatibility-${{ github.sha }}
          path: artifacts/compiler-compatibility.json
          if-no-files-found: error
          retention-days: 30

  grammar-repl-compatibility:
    runs-on: macos-15
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: swift-actions/setup-swift@v2
        with:
          swift-version: "6.1"
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Validate pinned Grammar-REPL without source changes
        env:
          ECOSYSTEM_REPOSITORIES: Grammar-REPL
          ECOSYSTEM_SKIP_WORKBENCH: 1
          ECOSYSTEM_REPORT_PATH: artifacts/grammar-repl-compatibility.json
        run: Scripts/validate-ecosystem-integration.sh
      - uses: actions/upload-artifact@v4
        with:
          name: grammar-repl-compatibility-${{ github.sha }}
          path: artifacts/grammar-repl-compatibility.json
          if-no-files-found: error
          retention-days: 30

  pinned-integration:
    runs-on: macos-15
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: swift-actions/setup-swift@v2
        with:
          swift-version: "6.0"
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Build pinned repositories and run shared conformance corpus
        env:
          ECOSYSTEM_REPORT_PATH: artifacts/ecosystem-integration-report.json
        run: Scripts/validate-ecosystem-integration.sh
      - name: Upload integration evidence
        if: always() && hashFiles('artifacts/ecosystem-integration-report.json') != ''
        uses: actions/upload-artifact@v4
        with:
          name: ecosystem-integration-${{ github.sha }}
          path: artifacts/ecosystem-integration-report.json
          if-no-files-found: error
          retention-days: 30
